# src

Runnable loop lands here in the next commits:

- `timing.py` — hop timers
- `turn_controller.py` — endpointing / barge-in hooks
- `adapters/` — STT / LLM / TTS
- `main.py` — one-command local session
