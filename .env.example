# Copy to .env — never commit secrets
# Live: python3 -m src.main --live --audio fixtures/hello.wav --write-session
#
# === Required for default live stack (hire-signal) ===
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
LLM_API_KEY=
#
# LLM hop only (swappable — not the Voice differentiator)
# LLM_PROVIDER=openai          # openai | anthropic
# LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY=              # alias accepted if LLM_API_KEY unset (openai)
# ANTHROPIC_API_KEY=           # alias if LLM_PROVIDER=anthropic
#
# Deepgram
# DEEPGRAM_MODEL=nova-2
#
# ElevenLabs
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
# ELEVENLABS_MODEL_ID=eleven_multilingual_v2
