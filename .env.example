# Copy to .env — never commit secrets
# Live mode: python3 -m src.main --live
STT_API_KEY=
TTS_API_KEY=
LLM_API_KEY=
# Optional provider hints (stubs ignore until wired)
# STT_PROVIDER=deepgram
# TTS_PROVIDER=elevenlabs
# LLM_PROVIDER=openai
