# fixtures

`hello.wav` — synthetic speech from espeak-ng for live STT smoke tests.

For portfolio metrics you care about, replace with a real mic capture of your own voice (`--audio path.wav`). Do not invent latency numbers from this fixture alone without labeling the source.
